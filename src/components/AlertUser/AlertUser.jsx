import React from 'react';

const AlertUser = (props) => {
    return ( 
        <div>
            <button onClick= {props.alertUser}>Click This!</button>
        </div>
     );
}
 
export default AlertUser;